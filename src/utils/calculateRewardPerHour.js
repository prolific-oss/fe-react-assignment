export default function calculateRewardPerHour({ rewardPerParticipant, timeInMinutes }) {
  // TODO: calculate reward per hour
  // rewardPerParticipant: £ per person
  // timeInMinutes: length of study
  return 1;
}
